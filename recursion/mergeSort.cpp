#include <iostream> 
#include <vector> 
using std::cout, std::cin, std::vector; 

void sortArary(vector<int>&);
vector<int> mergeSort(vector<int>&); 
vector<int> mergeSortedArray(vector<int>&,vector<int>&); 

int main()
{ 
    vector<int> arr = {2,5,1,6,1,5,7,17,1,5,6,2,0,5}; 
}

void sortArary(vector<int>& arr)
{ 
    
}
vector<int> mergeSortedArray(vector<int>& arr1,vector<int>& arr2)
{ 

}
vector<int> mergeSort(vector<int>& arr)
{ 

}